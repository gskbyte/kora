package org.gskbyte.kora.profilesActivities.deviceProfiles;

import org.gskbyte.kora.customViews.detailedListView.DetailedListViewAdapter;

import android.app.ListActivity;
import android.os.Bundle;

public class DeviceProfilesActivity extends ListActivity
{
    public void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        //DetailedListViewAdapter adapter = new DetailedListViewAdapter(this, R.layout.detailedlistview);
        //setListAdapter(adapter);
    }
}
